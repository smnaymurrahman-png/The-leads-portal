/**
 * @leads-portal/shared
 *
 * Types and constants shared between the NestJS API and the Next.js web app.
 * Keep this package free of runtime dependencies — it is imported by both
 * a Node backend and a browser bundle.
 */

export * from './roles';
export * from './http';
